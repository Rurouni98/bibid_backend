spring.application.name=board-app-backend

server.port=8080

# db ??
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://${DB_HOST}:6543/postgres
spring.datasource.username=postgres
spring.datasource.password=!dkdlxl1234

# Cloudflare R2
cloud.aws.credentials.access-key=${CLOUD_ACCESSKEY}
cloud.aws.credentials.secret-key=${CLOUD_SECRETKEY}
cloud.aws.s3.endpoint=https://58debf7da63e54ff2c36b1cf4c44d330.r2.cloudflarestorage.com
cloud.aws.s3.bucket.name=${CLOUD_NAME}

#filesize
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=100MB

# Swagger UI
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# JPA ??
spring.jpa.database=postgresql
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

live.ncp.accessKey=${LIVE_ACCESSKEY}
live.ncp.secretKey=${LIVE_SECRETKEY}
live.cloud.aws.s3.bucket.name=${LIVE_BUCKET_NAME}
live.ncp.profileName=LiveAuction

spring.data.redis.host=${SPRING_REDIS_HOST}
spring.data.redis.port=${SPRING_REDIS_PORT}
spring.data.redis.timeout=60000
spring.data.redis.database=0
spring.data.redis.password=${SPRING_REDIS_PASSWORD}

front.url=https://${FRONT_URL1}
kakao.client.id=${KAKAO_CLIENT_ID}
naver.client.id=${NAVER_CLIENT_ID}
naver.client.secret=${NAVER_CLIENT_SECRET}

cookie.secure=${COOKIE_SECURE}
